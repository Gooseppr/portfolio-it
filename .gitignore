# Notes & sources de travail (ne pas publier — seul le site va dans git)
portfolio_full_summary.md
portfolio_projects_metadata.json

# Outil interne d'extraction de fiches projet (local only)
PROMPT_EXTRACTION_PROJET.md
_fiche-portfolio.md

# Documents personnels (CV, profil) — ne pas publier sur un repo public
*.pdf

# OS / éditeurs
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Divers
*.log
*.tmp
node_modules/
